--- v0.2/sketch_aug21a_copy_20251102141514.ino
+++ v0.3/sketch_aug21a_copy_20251102141514.ino
@@ -33,7 +33,7 @@
 
   WiFi.begin(ssid, password);
   Serial.print("Conectando a Wi-Fi");
-  // v0.2 simplificado: while omitido (WiFi.status() != WL_CONNECTED) {
+  while (WiFi.status() != WL_CONNECTED) {
     Serial.print(".");
     delay(500);
   }
@@ -51,10 +51,7 @@
 }
 
 void loop() {
-  
-  // v0.2: heartbeat
-  Serial.println(\"tick\");
-server.handleClient();
+  server.handleClient();
 
   // --- Lectura sensores ---
   int valorBruto = analogRead(SensorPin);